import { TagifyDirective } from './tagify.directive';

describe('TagifyDirective', () => {
  it('should create an instance', () => {
    const directive = new TagifyDirective();
    expect(directive).toBeTruthy();
  });
});
